const {Server, WebSocket} = require("mock-socket")

describe('Heartbeat Socket', () => {
  test('Can connect and send messages', () => {
    expect(0).toBe(0)
  })

  test('Can connect and receive messages', () => {
    expect(0).toBe(0)
  })

  test('Can automatically reconnect', () => {
    expect(0).toBe(0)
  })

  test('Can close', () => {
    expect(0).toBe(0)
  })
})